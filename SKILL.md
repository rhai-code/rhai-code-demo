---
name: add-new-rhai-code-demo
description: Use this skill to add a new demo site
---

# Instruction

Read CLAUDE.md for new static site listings.

Update the Demos page with any new static site.

Update the Code page with the new GitHub repo link.

Generate a cool "wow factor" image for it.

Run `npm run dev` locally to preview the site.

Run `make build` to create minified static site.

Ask the user if they wish to check into git.
